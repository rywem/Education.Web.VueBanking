# Loaded by `vite` (dev). Relative base -> requests go to the Vite origin and
# get proxied to the .NET API (see vite.config.ts). Keeping it relative is why
# CORS doesn't bite in dev.
VITE_API_BASE_URL=/api
