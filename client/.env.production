# Loaded by `vite build`. If you deploy the SPA same-origin as the API (ASP.NET
# serving dist/), keep this relative. If the API lives on a different host,
# set the absolute URL here AND make sure that origin is in the CORS policy.
VITE_API_BASE_URL=/api
